// Реализуйте и экспортируйте по умолчанию каррированую версию функции,
// которая принимает на вход три строки и возвращает новую строчку составленную из трех входных.

function concat(a){
    return function (b){
        return function (c){
                return a+b+c;
        }
    }
}

// исходная версия
concat('a', 'bc', 'f'); // abcf

// каррированая версия
concat('a')('bc')('f'); // abcf